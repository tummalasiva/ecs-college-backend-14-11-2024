<html>
  <head>
    <style>
      body { width: 100%; margin-left: -2px; } .bb { border: 2px solid black;
      border-collapse: collapse; } table { width: 100%; } table,th,td { border:
      2px solid black; border-collapse: collapse; } td { text-align: center;}
      h3{ text-align: center; } text{text-align: center;}
    </style>
  </head>
  <body>
    <h3>
      {{setting.schoolName}}
    </h3>
    <text>[{{setting.address}}]</text>
    <table>
      <tr>
        <th> Sl </th>
        <th>Item Id</th>
        <th>Item Name</th>
        <th>Department</th>
        <th>Brand</th>

      </tr>
      {{#each data}}
        <tr>
          <td>{{inc @index}}</td>
          <td>{{this.itemId}}</td>
          <td>{{this.name}}</td>
          <td>{{this.department.department}}</td>
          <td>{{this.brand}}</td>

        </tr>
      {{/each}}
    </table>

  </body>
</html>